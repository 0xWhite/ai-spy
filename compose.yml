services:
  redis:
    image: redis:7-alpine
    container_name: ai-spy-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - ai-spy-redis-data:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  ai-spy-redis-data:
